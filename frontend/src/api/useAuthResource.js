import { useState, useEffect } from "react";

export const useAuthResource = (resourceUrl) => {
    const [resource, setResource] = useState([]);
    const [loading, setLoading ] = useState(false);



}